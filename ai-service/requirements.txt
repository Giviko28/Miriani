fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# RAG pipeline (Milestone 3). chromadb 1.x ships prebuilt (Rust) wheels — no C++ build
# toolchain required on Windows, unlike the 0.5.x line (chroma-hnswlib).
chromadb==1.5.9
langchain-text-splitters==1.1.2
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5

# Agentic system (Milestone 5): router + specialized agents via LangGraph.
langgraph==1.2.5

# External DB connector (text-to-SQL agent). SQLite driver is built-in;
# for PostgreSQL add psycopg2-binary, for MySQL add pymysql.
sqlalchemy==2.0.41

# Testing (Milestone 7).
pytest==9.1.0
