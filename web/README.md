# web — Frontend (React + Vite + TS + Tailwind + shadcn)

Scaffolded during **Milestone 6 (week 11)**, but a minimal page is wired earlier for the
integration smoke test. Calls the .NET API only — never the Python AI service directly.

Planned setup:

```bash
npm create vite@latest . -- --template react-ts
npm install
npm run dev
```
