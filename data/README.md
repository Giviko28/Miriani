# data — Demo seed documents

Sample company documents (policies, invoices templates, handbooks, etc.) used to seed the
RAG knowledge base for the single example organization. Uploaded files at runtime go to
`data/uploads/` which is git-ignored.
